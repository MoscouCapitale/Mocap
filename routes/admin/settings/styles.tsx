import StylesSettings from '@islands/Settings/StylesSettings.tsx';

export default function Styles() {
    return (
        <main className={"flex-col justify-center items-start gap-14 inline-flex"}>
            <StylesSettings />
        </main>
    );
}